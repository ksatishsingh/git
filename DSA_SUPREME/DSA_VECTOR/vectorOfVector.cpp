#include<iostream>
#include<vector>

using namespace std;

int main(){
    vector<vector<int> >arr;
    int row=5;
    int col=5;

    vector<vector<int>> vector_array(row,vector<int>(col,7));

    vector<int> a{1,2,3};
    vector<int> b{5,6,7};
    vector<int> c{10,11,12};

    arr.push_back(a);
    arr.push_back(b);
    arr.push_back(c);

    for()
}